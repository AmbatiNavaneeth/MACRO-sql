SECTION 11: Database Design & Security
101. Database normalization forms
102. 1NF, 2NF, 3NF
103. Star vs snowflake schema
104. OLTP vs OLAP
105. Data integrity
106. Referential integrity
107. Cascading actions
108. SQL injection
109. Preventing SQL injection
110. User roles & privileges
111. GRANT
112. REVOKE
113. Backup and restore
114. Database migration
115. Data consistency
116. Locking
117. Read vs write locks
118. Partitioning
119. Sharding (conceptual)


101. Database Normalization Forms
Normalization is the process of organizing data to reduce redundancy and improve data integrity.
           1. Reduce Data Redundancy
           2. Improve Data Consistency
           3. Save Storage Space
           4. Improve Database Structure


Bad Table:
EmpID	  Name  	DeptName
1	      Ravi	    IT
2	      John      IT

"IT" is repeated.

Output After Normalization

Employees
EmpID	  Name	DeptID
 1	  Ravi	  1
 2	  John	  1

Departments
DeptID	DeptName
  1	       IT



102. 1NF, 2NF, 3NF - 1NF removes repeating groups, 2NF removes partial dependency, and 3NF removes transitive dependency."

1NF - No repeating groups or multiple values in a column.
Bad Table                   
ID	Name	Phones
1	  Ravi	111,222

Good Table
ID	Name	Phone
1	  Ravi	111
1	  Ravi	222

2NF - Must be in 1NF and remove partial dependency.
Information should depend only on the main identifier of the row.

3NF - Must be in 2NF and remove transitive dependency


103. Star Schema vs Snowflake Schema - Star schema is simpler and faster, while snowflake schema is more normalized

-> A central fact table is directly connected to multiple, single-level dimension tables.
       Product
          |
Customer--Sales--Time
          |
       Store
Pros: Fewer JOINs, faster query performance, and highly intuitive for users to understand.
Cons: Higher data redundancy; difficult to update since a single change might affect many rows
use cases:
Business Intelligence (BI): Ideal for feeding data directly into dashboarding tools like Tableau or Power BI 
Real-time Analytics: perfect for read heavy operations

-> Dimension tables further normalized.
Product
   |
Category
   |
Sales

Pros: Saves disk space and ensures high data integrity with minimal redundancy.
Cons: Slower query performance due to multiple JOINs and highly complex SQL querying

Complex Data Hierarchies
Large Enterprise Warehouses: Best for massive datasets where storage costs are significant and data redundancy must be minimized

104. OLTP vs OLAP
OLTP (Online Transaction Processing) manages daily, real-time operational transactions (like bank withdrawals or e-commerce purchases). - ATM transactions, daily payments
OLAP (Online Analytical Processing) analyzes historical data to uncover business trends and insights. - yearly profits , turnover

105. Data Integrity
Data integrity is about ensuring the accuracy, consistency, and reliability of data throughout its lifecycle. In database management.
- enitity integrity - Ensures that each row in a table represents a unique entity. using primary key and unique
- referential integrity - Maintains logical and consistent relationships between different tables. It is established using FOREIGN KEY constraints
- Domain Integrity - Guarantees that all values in a specific column fall within a valid, defined range or data type. It is enforced using NOT NULL, DEFAULT, and CHECK constraints (e.g., ensuring a user's age is greater than 18
- User-Defined Integrity - Custom rules or business logic that are not covered by standard database constraints. These are typically implemented using Triggers or Stored Procedure

106. Referential Integrity
Ensures foreign key values always reference valid rows.


107. Cascading Actions
Cascading actions automatically propagate updates or deletes to child tables if there any changes for parent table 
When you connect two tables (like a Parent table and a Child table), cascading tells the database what to do to the child when the parent changes.

1. CASCADE (The Copycat)
)Whatever happens to the parent happens to the child.
Delete Parent: The child gets deleted too - Update Parent: The child updates to match.
example:- If you delete a User Account, their Profile Pictures are automatically deleted too.

2. SET NULL (The Disconnect)
The child stays, but loses the connection to the parent. 
Delete Parent: The child's link turns blank (NULL).
Example: If a Manager leaves the company, the Employees keep their jobs, but their "Manager" box is left blank.

3. RESTRICT / NO ACTION (The Blockade)
The database stops you from making changes.
Delete Parent: The database says "No!" and blocks the delete.
Example: You cannot delete a Product if a customer has already Ordered it. You must delete the order first.

108. SQL Injection
SQL injection is an attack where malicious SQL code manipulates database queries
 Attackers use this flaw to bypass authentication, access, modify, or delete sensitive data

SELECT * FROM Users WHERE Username = 'user_input' AND Password = 'password_input'
If an attacker enters ' OR 1=1 -- into the username field, the resulting query executed by the database becomes:SELECT * FROM Users WHERE Username = '' OR 1=1 --' AND Password = 'password_input'
Why this succeeds:
'       closes the username string.
OR 1=1  forces the condition to always evaluate to true.
--      (or # in some SQL dialects) comments out the rest of the query, ignoring the password check.
The database authenticates the attacker as the first user in the table, often the administrator.


109. Preventing SQL Injection
Methods
Prepared Statements
Parameterized Queries - A parameterized query (also known as a prepared statement) is a database query template that uses placeholders instead of embedding user inputs directly into the SQL code.
Input Validation

query = "SELECT * FROM users WHERE username = ?"
cursor.execute(query, (user_input,)) 

110. User Roles & Privileges
Roles and privileges restrict database access according to user responsibilities.
Control what users can do in a database.

To Check the users:    select * from MySQL.user;
To create a new users: create user_name@localhost identified by "password";
To rename usernames:   RENAME USER  old_user@localhost  TO  new_user@localhost;


111. GRANT
GRANT is used to provide previleegs (permissions) to users.
Giving all permissions to users for all databases and tables                              - grant all privileges on *.* to user_name@localhost;
Giving only select and update permissions to users for all databases and tables           -  grant select,update  on *.* to user_name@localhost;
Giving only select and update permissions to users for only particular database and table - grant select,update  on database_name.table_name to user_name@localhost;

112. REVOKE
REVOKE – Removes access privileges(permissions)  from users 
removing all permissions from users for all databases and tables                  - revoke all privileges on *.* from user_name@localhost;
Removing only drop and select permissions from users for all databases and tables - Revoke drop,select on *.* from user_name@localhost;
Removing all permissions from users for only selected database and tables         - Revoke all privileges on database_name.table_name from user_name@localhost;

113. Backup and Restore
Backup saves database copies. Restore recovers them.
A copy of data that you can use to restore and recover the data after a failure.

BACKUP DATABASE YourDatabaseName
TO DISK = 'C:\Backups\YourDatabaseName.bak'
WITH FORMAT, MEDIANAME = 'SQLServerBackups', NAME = 'Full Backup of YourDatabaseName';


RESTORE DATABASE YourDatabaseName
FROM DISK = 'C:\Backups\YourDatabaseName.bak'
WITH REPLACE;


114. Database Migration
Database migration generally refers to either Schema Migration - modifying table structures incrementally 
                                                Data Migration - transferring data from one database system to another


Example
MySQL → PostgreSQL


115. Data Consistency
Data consistency in SQL ensures that a database transitions only from one valid state to another, strictly obeying all predefined rules, data types, constraints, and triggers

Core Pillars of SQL Consistency
- ACID Transaction Properties 
- Database Constraints


116. Locking
Locks prevent multiple users from modifying the same data simultaneously.
Common Locking Challenges
1. Blocking
Blocking occurs naturally when one transaction holds a lock on a resource and a second transaction requests an incompatible lock on the same resource. 
The second transaction is forced to wait until the first commits or rolls back. Severe blocking can degrade application performance.
2. Deadlock
A deadlock happens when two transactions are blocked because each holds a lock that the other needs to proceed.
Example: Transaction A locks Row 1 and requests Row 2. Simultaneously, Transaction B locks Row 2 and requests Row 1.

117. Read vs Write Locks
Read locks allow multiple users to look at data at the same time
write locks  allow to a single user to modify data.

118. Partitioning
Partitioning divides large tables into manageable pieces that are called partitions - Splits data on a single server
Example
Sales table:
2024 Data
2025 Data
2026 Data
Stored separately.
Output
Faster Query Performance

1. Horizontal Partitioning -  A massive Orders table is split into 12 partitions by month
2. Vertical Partitioning - Splitting a Users table into two: one partition holds frequently accessed text columns (Username, Password), while the second holds rarely accessed binary data (Profile_Picture_BLOB


119. Sharding
Sharding is a database architecture pattern that cuts a single database into smaller, faster, and more manageable pieces called shards.


120. Multi-Tenant Databases
Multi-tenancy allows multiple customers to share the same database infrastructure securely. - Splits data on across multiple independent server
A SaaS company stores data for multiple clients in one database.

































































